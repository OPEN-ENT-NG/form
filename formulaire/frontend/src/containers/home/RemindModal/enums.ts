export enum DisplayContentType {
  WRITE_REMIND,
  NO_DISTRIBUTIONS,
  FOLLOW,
  LOADING,
}

export enum StatusResponseType {
  IS_ANSWERED_ACTIVE = "isAnsweredActive",
  IS_NOT_ANSWERED_ACTIVE = "isNotAnsweredActive",
}
