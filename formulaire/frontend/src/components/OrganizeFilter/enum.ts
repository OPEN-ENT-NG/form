export enum MenuItemState {
  ASCENDING,
  DESCENDING,
}

export enum ChipsID {
  SHARED = "shared",
  SENT = "sent",
  TODO = "todo",
  FINISHED = "finished",
}

export enum MenuItemsID {
  CREATION = "creation",
  MODIFICATION = "modification",
  TITLE = "title",
}
