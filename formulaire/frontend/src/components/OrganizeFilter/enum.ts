export enum MenuItemState {
  ASCENDING,
  DESCENDING,
}

export enum ChipsID {
  SHARED = "shared",
  SENT = "sent",
}

export enum MenuItemsID {
  CREATION = "creation",
  MODIFICATION = "modification",
  TITLE = "title",
}
